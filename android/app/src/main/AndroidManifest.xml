<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Special "usage access" permission needed to read real screen time via UsageStatsManager.
      This cannot be requested through a normal runtime permission dialog: the user must grant
      it manually in Settings (see UsageStatsModule.openUsageAccessSettings). Declaring it here
      is what makes the OS recognize a grant made that way.
    -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!--
      Package visibility (Android 11+/API 30+): without declaring this, queryIntentActivities()
      in InstalledAppsModule.kt would only see a small allowlisted set of apps instead of
      everything with a launcher icon.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep link used by BlockingActivity to redirect back into the blackjack table. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="screentimecasino" />
        </intent-filter>
      </activity>

      <!--
        Full-screen interrupt shown when a managed app whose time is exhausted is opened. Plain
        Activity, no RN dependency, runs in the same isolated process as the accessibility
        service so it works even if the main RN process is dead.
      -->
      <activity
        android:name=".blocking.BlockingActivity"
        android:process=":blocking"
        android:exported="false"
        android:launchMode="singleInstance"
        android:taskAffinity=""
        android:excludeFromRecents="true"
        android:theme="@style/AppTheme" />

      <!--
        Watches foreground-app changes system-wide to detect when a managed, time-exhausted app
        is opened. Runs in its own process for crash isolation from the main RN process (see
        MainApplication's process guard). exported="true" is required for system_server to bind
        it at all, despite BIND_ACCESSIBILITY_SERVICE also being declared.
      -->
      <service
        android:name=".blocking.BlockAccessibilityService"
        android:process=":blocking"
        android:exported="true"
        android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
        <intent-filter>
            <action android:name="android.accessibilityservice.AccessibilityService" />
        </intent-filter>
        <meta-data
            android:name="android.accessibilityservice"
            android:resource="@xml/accessibility_service_config" />
      </service>
    </application>
</manifest>
